# Memory Log - 2026-07-13

## What Happened
- Synchronized and updated the lead flow with 22 leads from July 8 to July 13, 2026.
- Updated `src/components/crm/LeadFlow.tsx` by modifying the `REAL_LEADS` static array to include the new leads and update existing leads:
  - Marked **DATOYA Guest House (Client 23)** as `won` with a deal value of `Rp1.500.000`.
  - Marked **THE SUBAHU VILLA (Client 26)** as `won` with a deal value of `Rp2.500.000`.
  - Updated **Client 5 - Iwan Batu Malang** with the custom package deal value of `Rp3.100.000` and status as `interested` (pertimbangan).
- Created a database synchronization script (`scratch/sync_leads.ts`) to connect to Supabase and upsert all 22 leads into the database.
- Executed the synchronization script to successfully upsert all leads into the database.
- Deleted the temporary synchronization script after execution.
- Added 4 new inbound leads from July 9-10, 2026 (`Villa Bali 3 Kamar - nomor akhir 8081`, `Client 33 - nomor akhir 6077`, `Client 34 - nomor akhir 7563`, and `Client 35 - nomor akhir 0228`) to both `REAL_LEADS` array and the remote Supabase database.
- Verified compilation and type-safety using `npx tsc --noEmit`.
- Cloned the `JuliusBrussee/caveman` repository, extracted its seven core agent skills (`cavecrew`, `caveman`, `caveman-commit`, `caveman-compress`, `caveman-help`, `caveman-review`, `caveman-stats`), and successfully installed them globally into the Global Customizations Root (`C:\Users\lenovo\.gemini\config\skills`). Cleaned up temporary files.

## Lessons Learned
- Synchronizing static frontend fallback lists (like `REAL_LEADS` array) alongside remote data stores (like Supabase) ensures a consistent layout regardless of whether the user is viewing offline/fallback or live synced data views.
- Validating the presence of phone numbers (WhatsApp contacts) in the database before inserting new leads avoids duplicate records and preserves agent-specific notes.

