# Memory Log - 2026-09-15

## What Happened
- Audited the entire WelliBuilds-CRM project using `vibe-code-auditor` and `project-skill-audit`.
- Fixed 4 blocking ESLint errors and resolved all lint failures across:
  - `src/components/crm/CEODailyFocusPanel.tsx` (fixed `prefer-const` on `updatedNeedAcc`, removed unused imports).
  - `src/components/crm/LeadFlow.tsx` (escaped JSX quotes `&quot;`, cleaned up 9 unused icon imports and dead variables).
  - `src/components/layout/InstallPwaButton.tsx` (escaped apostrophe `&apos;`).
  - `src/components/layout/AppShell.tsx` (eliminated duplicate `<Toaster />` declaration).
  - `eslint.config.mjs` (added `scratch/**` to `globalIgnores`).
- Fortified `src/app/api/pipeline-agent/route.ts` with Bearer token authentication (`verifyAgentAuth`), same-origin permission, and configurable working directory `HERMES_PIPELINE_CWD`.
- Made `src/lib/supabase.ts` resilient by preventing top-level unhandled exception throws when environment variables are unpopulated.
- Synchronized project metadata: updated `package.json` name to `wellibuilds-crm` and updated `README.md` to document Next.js 16 App Router + Turbopack.
- Verified TypeScript compilation (`npx tsc --noEmit` -> 0 errors), ESLint (`npm run lint` -> 0 errors), and production build (`npm run build` -> successful build with Turbopack).
- Configured Supabase MCP server (`project_ref=nsjitxfhrsjczcplycyc`) across `~/.gemini/config/mcp_config.json`, `~/.gemini/antigravity-ide/mcp_config.json`, and `~/.gemini/antigravity/mcp_config.json`.
- Installed Supabase Agent Skills (`supabase` and `supabase-postgres-best-practices`) locally into `.agents/skills` and globally into `~/.agents/skills`.

## Lessons Learned
- Unhandled top-level errors in client-side singleton initializers (e.g. Supabase client) can break module bundling and SSR pre-rendering even before the app starts.
- Hardcoded local paths and unauthenticated execution in Next.js API routes prevent safe serverless deployments; always sanitize execution contexts with environment variables and token verification.
- Keeping MCP configs synchronized across configuration roots ensures consistent tooling availability across IDE restarts and agent sessions.
